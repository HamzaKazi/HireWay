<%# <section class="Card">
  <div class="container">
    <h2 class="card-heading">All Vehicles</h2>
    <div class="row">
      <% @vehicles.each do |vehicle| %>
        <%# <div class="col-sm-6 col-md-4">
          <%= render 'shared/card', vehicle: vehicle %>
        <%# </div>
      <% end %>
    <%# </div>
  </div>
</section> %>
<%# <div> %>
  <section id="header" class="jumbotron text-center">
  <%= form_with url: vehicles_path, method: :get, class: "d-flex" do %>
    <%= text_field_tag :query,
      params[:query],
      class: "form-control",
      placeholder: "Find a vehicle"
    %>
    <%= submit_tag "Search", class: "btn btn-primary" %>
  <% end %>
  <br>
      <h1 class="display-3">Luxury Vehicles</h1>
      <p class="lead">Discover luxury vehicles below and hire the car / boat of your dreams.</p>
  </section>
<%# </div> %>

<%# <div class ="split left"> %>
  <section id="gallery">
  <br>
  <h2> Current Listings: </h2>
    <div class="container">
      <div class="row" style="width: 100%;">
          <% @vehicles.each do |vehicle| %>
          <%# <% if vehicle.category == "Yacht" then%>
             <div class="col">
            <%= link_to  vehicle_path(vehicle) do %>
                <div class="card" style="width: 20rem;">
                   <%# <img class="card-img-top img-fluid" src="" alt="" class="mx-auto d-block"> %>
                     <%=cl_image_tag vehicle.image.key, class: "mx-auto d-block card-img-top img-fluid" %>
                  <div class="card-body">
                    <h5 class="card-title" style="color:black;"><%= vehicle.name%></h5>
                    <p class="card-price" style="color:black;">Price: £<%= vehicle.price%> p/hr</p>
                  </div>
                </div>
            <%# <% end %>
            <% end %>
            </div>
          <% end %>
        </div>
    </div>
  </section>
<%# </div> %>
<%# <div class ="split right"> %>
<div class="map">
        <div style="width: 100%; height:1200px;"
          data-controller="map"
           data-map-markers-value="<%= @markers.to_json %>"
          data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
        </div>
</div>
<%# </div> %>
<%# <%= cl_image_tag("sxheg955syqgeeu5qflo",
      width: 400, height: 300, crop: :fill) %>
