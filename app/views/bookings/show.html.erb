<div class="map" style="background-image: url('<%= @vehicle.image %>')"></div>

<div class="container margin-top-negative">
  <div class="row">
    <div class="col-8">
      <div class="vehicle-show-card">
        <h1>Vehicle: <%= @booking.vehicle.name %></h1>
<%= cl_image_tag @booking.vehicle.image.key %>
      </div>
      <div class="vehicle-show-card">
              <h3>Details:</h3>
        <h3>Email: <%= @booking.user.email %></h3>
        <br>
        <h3>Date: <%= @booking.date[13] %> / <%= @booking.date[19..20] %> / <%= @booking.date[4..7] %> </h3>
     </div>
    </div>

    <div class="col-4">
      <div class="vehicle-show-card">
          <div class="input-group">

      <div class="vehicle-show-card">
        <div class="flex">

          <br>
                  <h3>Hours: <%= @booking.hours %></h3>
            <br>
            <br>
</button>
    <button> <%=  link_to "DELETE", booking_path, class: "btn btn-cancel", data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this list?" } %> </button>
    </div>
  </div>

          <div class="flex">
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
